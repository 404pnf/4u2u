<?php
// $Id$
/**
* @file
* Lets users add private annotations to nodes.
*
* Adds a text field when a node is displayed
* so that authenticated users may make notes.
*/
function online_game_menu() {
	
	
 $items['game/link']=array(
     'title' => 'play game',
     'page callback' => 'game_url',
     'type' => MENU_CALLBACK,
   );
   
     return $items; 
}

function game_url()
{
   global $user;

   $sql = "SELECT sid FROM {sessions} where uid = %d";
   $sid = db_result(db_query($sql,$user->uid));
   $url = "http://localhost/project/jiaxiaoquan/game/index.php".'?sid='.$sid;
  	drupal_goto($url);

 	
/*	$brow = browinfo();
	if($brow == "Internet Explorer 6.0"){
		echo   "<SCRIPT   LANGUAGE='javascript'>";  
		echo   "window.open('$url')";    
		echo   "</SCRIPT></head></html>";   
	}else 
	{
		drupal_goto($url);
	}
   */
}